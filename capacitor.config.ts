import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'usjp.classattendance.app',
  appName: 'usjp-class-attendance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
